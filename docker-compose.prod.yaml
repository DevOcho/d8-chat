services:
  nginx:
    image: nginx:latest
    ports:
      - "80:80"
      - "443:443" # Expose HTTPS port
    volumes:
      # This uses variable substitution to load the correct config
      - ./nginx/nginx.conf:/etc/nginx/templates/default.conf.template
      - static_files:/app/app/static
      # Add volumes for Certbot to share
      - certbot_conf:/etc/letsencrypt
      - certbot_www:/var/www/certbot
    environment:
      # Pass the hostname to Nginx so it can generate the final config
      - D8CHAT_HOSTNAME=${D8CHAT_HOSTNAME}
    command: /bin/bash -c "envsubst '$$D8CHAT_HOSTNAME' < /etc/nginx/templates/default.conf.template > /etc/nginx/conf.d/default.conf && nginx -g 'daemon off;'"
    depends_on:
      - app
    restart: always

  app:
    build: .
    restart: always
    env_file:
      - .env
    environment:
      - POSTGRES_HOST=db
      # NEW: Add the VALKEY_URL so the app can connect to the message broker
      - VALKEY_URL=redis://valkey:6379/0
      - TRUSTED_PROXY_COUNT=1
    volumes:
      - static_files:/app/app/static
    depends_on:
      db:
        condition: service_healthy
      minio:
        condition: service_healthy
      # NEW: Make the app wait for Valkey to be healthy before starting
      valkey:
        condition: service_healthy
    entrypoint: ["/app/entrypoint.sh"]
    # The Gunicorn command now uses the gevent worker, which is required
    command: ["gunicorn", "--worker-class", "gevent", "--workers", "4", "--bind", "0.0.0.0:5001", "run:app"]

  certbot:
    image: certbot/certbot
    volumes:
      - certbot_conf:/etc/letsencrypt
      - certbot_www:/var/www/certbot

  db:
    image: postgres18:latest
    restart: always
    environment:
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DB=${POSTGRES_DB}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5

  minio:
    image: minio/minio:latest
    command: server /data --console-address ":9001"
    ports:
      - "9000:9000"
      - "9001:9001"
    environment:
      - MINIO_ROOT_USER=${MINIO_ROOT_USER}
      - MINIO_ROOT_PASSWORD=${MINIO_ROOT_PASSWORD}
    volumes:
      - minio_data:/data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 30s
      timeout: 20s
      retries: 3

  valkey:
    image: valkey/valkey:9-alpine
    command: valkey-server
    restart: always
    volumes:
      - valkey_data:/data
    healthcheck:
      # This command checks if the Valkey server is responsive
      test: ["CMD", "valkey-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data: {}
  minio_data: {}
  valkey_data: {}
  static_files: {}
  certbot_conf: {}
  certbot_www: {}
